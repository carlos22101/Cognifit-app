name: cognifit
description: CogniFit - Plataforma educativa docente

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  geolocator: ^13.0.2

  device_preview: ^1.2.0

  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_secure_storage: ^9.2.2


flutter:
  uses-material-design: true
